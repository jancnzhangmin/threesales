<%= form_for(systemlog) do |f| %>
  <% if systemlog.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(systemlog.errors.count, "error") %> prohibited this systemlog from being saved:</h2>

      <ul>
      <% systemlog.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :table %>
    <%= f.text_field :table %>
  </div>

  <div class="field">
    <%= f.label :userid %>
    <%= f.number_field :userid %>
  </div>

  <div class="field">
    <%= f.label :userip %>
    <%= f.text_field :userip %>
  </div>

  <div class="field">
    <%= f.label :textout %>
    <%= f.text_area :textout %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
